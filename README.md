# Immediate Mode Stroke Renderer in Metal

The system is built into two different layers
The low leve is `Renderer` which expects a bunch of colored verticies every frame.
On top of this there is are a bunch of functions that will generate the verts for particular geometries 
